version: "2.4"

# do not edit this file (pycharm_debugger.yaml) it is maintained by pycharm_debugger_port_proxy.sh

services:
    pycharm_debugger_proxy:
        image: tecnativa/whitelist
        networks:
            default:
            public:
        ports:
            - "127.0.0.1:12345:12345" #dynamic pycharm debugger port mapping
        environment:
            PORT: "12345" #dynamic pycharm debugger port connection
            TARGET: odoo
            PRE_RESOLVE: "False"

networks:
    default:
        internal: true
    public:
